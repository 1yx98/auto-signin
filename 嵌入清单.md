# 嵌入主程序 + 删除补丁包 —— 操作清单

> **触发条件（用户 2026-09-17 22:16 明确）**：
> **「确定补丁包生效且没问题，再删加嵌入」**
>
> 也就是说：**不是"跑了几次没报错"就嵌入**，而是**必须先证明每条补丁真的被触发过、
> 且没有副作用**。证明工具见下面第零节。

**原则**：补丁是**试用期形态**，不是永久形态。嵌入 = 把补丁代码"转正"进 `signin.py`，
然后删掉补丁包目录。**做完之后 `signin.py` 功能与嵌入前逐字节等价**
（唯一差别是补丁代码里的注释标记会被清掉）。

---

## 零、★ 先过这一关：补丁生效核查（新增，2026-09-17）

**双击 `检查补丁生效.bat`**（或 `runtime\python.exe 检查补丁生效.py 20`）。

它会扫最近的 run 日志，**按"补丁前/补丁后"分开统计**，报告：

| 栏目 | 含义 |
|---|---|
| 【一】每个补丁被触发过几次 | **只有「补丁后」的次数才算验证** |
| 【二】旧病反证 | 旧病那些串在**补丁后**应该 0 次 |

### 判定标准（硬门槛）

| 情况 | 能不能嵌入 |
|---|---|
| 某条补丁「补丁后有触发」且没引发新问题 | ✅ **可以嵌入** |
| 某条补丁一次都没触发（还没遇到那个场景） | ⚠ **还不能嵌入** —— 没验证过 |
| 旧病反证在补丁后**仍然出现** | ❌ **不能嵌入**，先查原因 |
| `smoke_test` 不是 158/158 | ❌ 不能嵌入 |

> ★ **为什么这么严**：2026-09-17 装完 3 个补丁后手跑一次，脚本**成功进入微信**，
> 看起来一切正常 —— 但事后一查，`跳过僵尸窗口` 触发 **0 次**
> （那次微信是完全关闭的，根本没有僵尸窗口可过滤）。
> **"跑通了"和"补丁生效了"是两件事。**
> 光凭"没报错"就嵌入，等于把一段从未被真正执行过的代码转正。

### 各补丁需要等什么场景才会被触发

| 补丁 | 需要等到的场景 | 触发时会出现的日志 |
|---|---|---|
| ① 滚动等页面变化 | 任何一次导航到详情页 | `页面已滚动` |
| ② 跳过无效滚动 | 一次**已签到态**下的第 4 步 | `滚动前预检` |
| ③ 僵尸窗口过滤 | 微信**退出/崩溃后残留窗口**时 | `跳过僵尸窗口 hwnd=` |
| ④ 进程检测 | `tasklist` 查询失败时 | `tasklist 查不到微信` |
| ⑤ 进入微信判定 | 「进入微信」按钮匹配失败时 | `不能据此断定` |
| ⑥ 不在区域内重跑 | **定位漂到校外**时 | `检测到灰色「不在区域内」` |
| ⑦ 记录日期判定 | **时段外看到灰色已签到**时 | `页面「签到时间」读到` |

**③④⑤⑥⑦ 都是"偶发场景"** —— 可能几天都不出现。
**所以"等它自然出现"可能要等很久。**
若想加速验证，可以**在安全的时机手动制造那个场景**（例如手动把微信关掉、
或故意让定位漂移），跑一次看日志。**不要为了验证而破坏正常签到。**

---

## 一、执行前必须满足的检查

| 检查 | 怎么看 | 不满足就别做 |
|---|---|---|
| ① **补丁生效核查通过**（第零节） | `检查补丁生效.bat` | 有"尚未验证"或"旧病仍在" → 别嵌入 |
| ② 连续几天自动任务无异常 | 台账 / 飞书 / `logs/run_*` | 有异常 → 先回滚排查 |
| ③ `smoke_test` 158/158 | `runtime\python.exe smoke_test.py` | 有 fail → 先修 |
| ④ 当前 `signin.py` sha = `2cc20645…` | 见下方命令 | 不是 → 说明有人动过文件，先弄清 |

```
runtime\python.exe -c "import hashlib;print(hashlib.sha256(open('signin.py','rb').read()).hexdigest())"
```
应输出 `2cc20645f4b99f995788262f8d14fbff11be223e033b20b9485aa11ff28e379d`

> ⚠ **只嵌入已成熟的那两个包（①②）**，别和 ③④⑤⑥⑦ 一起嵌入 ——
> 那样万一出问题分不清是谁的锅。

---

## 二、★ 嵌入前**必须**先搬护栏（硬门槛！）

**为什么**：`补丁包/test_patch_engine.py` 的 **9 条负向测试**里，有几条守的是
**补丁本身的正确性**（尤其"滚动指纹**不参与**成功判定"这条）。
补丁包一删，那些断言**一起消失** → 嵌入的代码立刻开始**裸奔**。

所以顺序是：**先把该守的断言搬进 `smoke_test.py`，再嵌入，最后删包。**

### 要搬的断言（按优先级）

| # | 断言内容 | 为什么必须搬 |
|---|---|---|
| 1 | **`_scroll_fingerprint` 不参与成功/失败判定** | 这是 P1 的安全底线。指纹只是个"页面动没动"的传感器，一旦有人后来把它接进判定逻辑，就会出现"因为画面抖动就判成功"的假成功 |
| 2 | **`_already_signed_detail` 的返回值不得被用作成功/失败结论** | P2 的安全底线。它只决定"要不要滚"，**不得**决定"签没签到" |
| 3 | **`_already_signed_detail` 返回 False 时流程等价于原行为** | 保证"探不到就退化"这个承诺不退化 |
| 4 | 两个新函数都有 `except` 兜底且**返回 False**（不是 `True`/`None`） | 异常时必须是"探不到"，不能是"探到了" |

> 按项目铁律 R1：这些断言**一律用 AST 查真实节点**，不许用字符串/正则。
> 按铁律 R3：**写完立刻跑一次**，再**故意改坏它守护的代码**看是否 FAIL。
> 没做过"改坏必 FAIL"验证的断言**不算存在**。

---

## 三、嵌入的具体位置（已核实）

### P1a —— `_scroll_fingerprint()` 函数

| 项 | 值 |
|---|---|
| 起始标记行 | **L2152** `# ===== 补丁 P1a（2026-09-16）：滚动指纹 =====` |
| 函数定义 | **L2161** `def _scroll_fingerprint(hwnd):` |
| 结束 | 下一个顶层 `def` 之前 |
| 插入位置 | 与 `_already_signed_detail` 相邻，都在 `open_signin_entry()` 定义之前 |

### P2a —— `_already_signed_detail()` 函数

| 项 | 值 |
|---|---|
| 起始标记行 | **L2180** `# ===== 补丁 P2a（2026-09-16）：已签到态预检 =====` |
| 函数定义 | **L2193** `def _already_signed_detail(hwnd):` |

### P1b —— 滚动后确认页面真的动了

| 项 | 值 |
|---|---|
| 标记行 | **L2302** `# 【补丁P1b·2026-09-16】滚动后必须确认"页面真的动了"再匹配。` |

### P2b —— 滚动前预检

| 项 | 值 |
|---|---|
| 位置 | **L2292–L2296**（在 `for k in range(6):` 循环体最前面） |
| 代码 | `if k == 0 and _already_signed_detail(...):` → log → shot → `break` |

### 嵌入要做的事

1. **删掉两行分隔标记**（`# ===== 补丁 P1a…` / `# ===== 补丁 P2a…`）——
   它们只是补丁引擎的定位锚点，嵌入后没用了。
2. **把 `【补丁P1b·2026-09-16】` / `【补丁P2b·2026-09-16】` 这类标记
   改成普通注释**（保留说明价值，去掉"这是补丁"的记号）。
3. **代码逻辑一个字都不改**。

> ⚠ **绝对不要动 L3765 / L3780 附近的「WiFi认证补丁」注释** ——
> 那是历史遗留的另一处修改，与本次两个补丁无关，**改名会误导以后的人**。

---

## 四、执行顺序（照抄）

```
:: 0) 先确认状态
runtime\python.exe -c "import hashlib;print(hashlib.sha256(open('signin.py','rb').read()).hexdigest())"
runtime\python.exe smoke_test.py

:: 1) 备份！（嵌入前一定先留一份）
copy signin.py _embed_backup_signin_patched.py

:: 2) 手工编辑 signin.py：按第三节删标记、改注释（逻辑不动）

:: 3) 跑测试，必须 158/158
runtime\python.exe smoke_test.py

:: 4) 功能等价性核对：嵌入后的代码应与嵌入前逐字节相同（除注释）
::    —— 如果第 2 步只动注释/标记，这一步能直接证明
::    （若第 2 步不小心动了逻辑，这里会发现）

:: 5) 确认无误 → 删除两个补丁包目录
::    注意：本沙箱删不掉目录（回收站 API 被拦），请你手动删：
::      删 D:\签到系统\补丁包\
::      删 D:\签到系统\跳过无效滚动补丁包\
::      删 D:\签到系统\.patch_packages.json
::      删 D:\签到系统\_embed_backup_signin_patched.py（确认无误后再删）
```

**第 4 步怎么核对逐字节等价**：因为第 2 步只该动注释/标记，
所以可以用"**去掉注释后比较 AST**"来证明逻辑没变：

```
runtime\python.exe -c "
import ast,hashlib
def norm(p):
    t=ast.parse(open(p,encoding='utf-8').read())
    for n in ast.walk(t):
        if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef,ast.ClassDef,ast.Module)):
            if (n.body and isinstance(n.body[0],ast.Expr)
                and isinstance(n.body[0].value,ast.Constant)
                and isinstance(n.body[0].value.value,str)):
                n.body.pop(0)          # 去掉 docstring
    return ast.dump(t)
a=norm('_embed_backup_signin_patched.py'); b=norm('signin.py')
print('AST 等价（去 docstring 后）:', a==b)
"
```
输出 `True` → **逻辑逐节点等价**，嵌入没改任何行为。

---

## 五、删包之后要补的事

1. **`smoke_test.py` 里新增的护栏断言**（第二节那 4 条）要能**独立通过**
   （不能再依赖补丁包的 `test_patch_engine.py`）。
2. **`README.md` 要更新**：
   - §7 整章标注为「**已毕业**」——补丁包已删除，机制仅作历史记录保留；
   - §14.1 状态表：`signin.py` sha 改为嵌入后的新值，删掉"进行中的实验"一行；
   - §14 变更日志追加嵌入日期与 sha。
3. **`.patch_packages.json` 要删**（否则以后有人再建补丁包会看到过期记录）。
4. **别删 `logs/` 里任何东西** —— 嵌入前的真实跑记录是以后排查的一级依据。

---

## ★★ 五之二、嵌入 P1/P2 之后，必须给另外 3 个新包「重定基线」

> **2026-09-17 晚新增了 3 个补丁包**（`僵尸窗口过滤补丁包` / `进程检测补丁包` /
> `进入微信判定补丁包`）。它们的**基线里包含 P1/P2 的补丁标记**。

**问题**：嵌入 P1/P2 会改变 `signin.py`（把补丁标记注释清掉），
于是这 3 个包的基线**对不上了** —— 拆卸时会被"自查判据"拦住。

**所以嵌入 + 删掉 P1/P2 那两个包之后，紧接着做这 4 步**：

```
:: 1) 删掉 3 个新包的状态记录
del 僵尸窗口过滤补丁包\_state.json
del 进程检测补丁包\_state.json
del 进入微信判定补丁包\_state.json

:: 2) 用「嵌入后的 signin.py」覆盖它们的基线备份
copy signin.py 僵尸窗口过滤补丁包\_backup\signin.py.base
copy signin.py 进程检测补丁包\_backup\signin.py.base
copy signin.py 进入微信判定补丁包\_backup\signin.py.base

:: 3) 从注册表里清掉这 3 个包的旧条目
::    （手工编辑 .patch_packages.json，把这三个键删掉；
::      如果整个注册表只剩它们，直接删掉 .patch_packages.json）

:: 4) 按顺序重新装（锚点是 AST 函数级，嵌入只删注释，锚点仍能定位）
runtime\python.exe 僵尸窗口过滤补丁包\patch_engine.py apply
runtime\python.exe 进程检测补丁包\patch_engine.py apply
runtime\python.exe 进入微信判定补丁包\patch_engine.py apply
```

**第 4 步之后必须核对**：`runtime\python.exe smoke_test.py` → **158 项全过**。

> ⚠ **另外**：这 3 个新包**从未在真实签到里跑过**（它们是 09-17 晚才建的）。
> 建议**先让它们跑几天**，确认真的解决了"任务栏弹窗"再考虑嵌入。
> **不要和 P1/P2 一起嵌入** —— 那样万一出问题，分不清是谁的锅。

---

## 六、如果嵌入后发现有问题

```
:: 用第 1 步的备份还原
copy _embed_backup_signin_patched.py signin.py
```
（注意：这是**嵌入前**的文件，仍然是"装着 P1+P2"的状态。
要回到更早的版本另有两条路：补丁包里还留着 `_backup/signin.py.orig` = 真正的原版
`af386517…`。但补丁包删了之后就只剩 `_embed_backup_signin_patched.py` 这一份了
—— **所以第 1 步的备份别省。**）

---

## 七、今天（09-16）现在能做什么

**不能做嵌入** —— 要等 09-18 结果。

现在可以做的：**照常手动跑一次**（若已过时间窗会以 `not_time` 退出码 3 结束，
**这是正常状态**）。它的价值是确认"P2 补丁在真实环境里起来不报错"。
