# 西安石油大学 油学通 自动签到系统（微信 PC 端）

面向西安石油大学「油学通」小程序的每日自动签到工具。

每天定时自动打开微信 → 搜索并进入「油学通」小程序 → 顺着「日常管理 → 签到消息 → 每日签到 → 进入按钮」多级入口进到签到详情页（详情页"签到" → 地图定位页"完成签到" → 硬确认"已签到"）。是否关机由 `config.json` 的 `shutdown_after_success` 决定（默认 `false`，不关机）。

> ⚠️ **换台电脑到手必做 3 件事（不做跑不起来）**：
> 1. 双击 `1_采集模板.bat`，重新采集 **油学通专属的 5 个模板**：搜索结果里的「油学通小程序图标」、工作台「日常管理」入口、「签到消息」入口、「每日签到」入口、点「每日签到」后进入签到页的那个「进入按钮」（模板与分辨率/缩放绑定，换机器无法复用）。另有 2 个微信通用模板（进入微信确认键 / 搜索框），界面没变可输 s 跳过。
> 2. 签到时间已按 鄠邑校区「20:50 开始签到」设好，定时触发 20:55；如需调整，改 `config.json` 的 `signin_time_start`/`signin_time_end`，并同步改 `install_task.ps1` 的 `$triggerTime`。
> 3. 双击 `2_手动测试签到(管理员).bat` 完整跑一遍，确认能进详情页、能定位、能走到"已签到"。

## 一、它是怎么工作的（signin.py）

1. **清障**：自动识别并关闭抢占前台的「屏幕外幽灵弹窗」（典型如联想电脑管家 lxwp 的高权限弹窗，会导致所有模拟点击落空）。需要脚本以管理员权限运行才能结束这类高权限进程。
2. **启动/唤醒微信**：没开就启动；最小化了会自动还原；停在「进入微信」确认页就自动点进去。进入后**等待界面真正渲染完成**（微信 4.x 启动时可能整块白屏，脚本会检测白屏并自动重绘/重启微信，绝不拿白屏去操作）。更新说明弹窗只在**同时识别到弹窗标题**时才点其右上角 X，避免误点微信主窗自身的关闭钮。
3. **搜索小程序**：用图像模板精准定位顶部搜索框（不写死坐标，微信窗口大小不同都能点中）→ 粘贴「油学通」→ 在「最近使用过的小程序」里用**多尺度图像匹配**找到 油学通 图标并点击。打开前会先清掉残留的旧小程序进程，保证每次从工作台首页进入（不会恢复到上次的详情页）。**全程绝不按 Esc**——实测微信 4.x 按一次 Esc 主界面就会整块白屏且无法自恢复。
4. **逐级进入签到详情页**：按 config 的 `signin_nav_steps` 顺序一路点进去——工作台「日常管理」→「签到消息」→「每日签到」→「每日签到里的进入按钮」→ 签到详情页（每一步都是首屏找不到就小步向下滚动；图标由各自模板定位，务必按油学通实际界面重新采集）。
5. 多级入口的最后一步「进入按钮」点进去后就到**签到详情页**（左边蓝色"签到"、右边绿色"请假"），无需中间的任务卡片点击。
6. **签到（两步页面）**：
   - **详情页**：只在窗口下半部动态扫描宽幅按钮，自动排除顶部蓝色标题栏——蓝色「签到」=可签（绿色「请假」不会误点）；灰色「已结束」=不在时段或已签，不点击、不关机。
   - **地图定位页**：点蓝色签到后进入地图页，等定位完成、底部「完成签到」由灰变**绿**后点击；若弹出「签到成功,是否返回?」自动点「确定」。
7. **关机**：判定签到成功后，按 `config.json` 的 `shutdown_delay`（默认 60 秒）倒计时关机。

> 全程不写死像素坐标：搜索图标用多尺度模板匹配、签到按钮按颜色和形状动态扫描，微信窗口大小不同也能识别。
>
> **日志按运行次数归档**：每次运行在 `logs/run_年月日_时分秒/` 里单独存一份 `run.log` 和该次全部过程截图，自动保留最近 30 次（`config.json` 的 `keep_runs` 可调）；`logs/signin_当天.log` 是当天所有运行的汇总，定时任务另写 `logs/task_run.log`。排查问题时直接看最新的 `run_*` 文件夹即可。

### 失败可见性（不想天天盯日志，就看这里）

签到脚本最怕的不是"失败"，而是"失败了你不知道"。为此有三道机制：

1. **开跑即通知**。到点启动后立刻推一条蓝色「开始执行」卡片。如果只有"开始"、迟迟没有结果卡片，
   说明流程卡住了（全局超时 900 秒后会兜底判失败并发失败卡）。
2. **每次结果都推飞书**，并且卡片底部附一行 **`📊 近 7 天：成功 N、失败 M；成功率 X%`**，
   不用翻日志就知道最近状态。
3. **连续失败会升级告警**。当"连续失败 ≥2 天"或"近 7 天失败 ≥3 次"时，
   卡片标题直接变成红底 **「连续失败！」**，并附上"该检查什么"的提示——
   单次失败可能只是偶然，连续失败才是真出问题了。

### 本地台账（data/signin_history.csv）

每次运行结束（**包括异常退出**）都会追加一行到 `data/signin_history.csv`，字段：
日期、时间、结果、退出码、星期、耗时、失败步骤、失败原因码、运行目录、备注。

- 用 Excel 直接双击打开，不会乱码（UTF-8 BOM）。
- 同一天跑了多次（含重试轮）只按"当天最好结果"统计一次，重试成功不会被记成失败。
- **纯追加，从不改写历史行**；万一文件格式对不上，会另存一个带时间戳的新文件，不丢旧数据。
- 台账写入失败只打一条 warning，**绝不影响签到主流程**。
- `data/` 已在 `.gitignore` 中排除，不会随项目上传。

### 三轮重试

时间窗（默认 20:50–21:30）内最多走三轮：第 1→2 轮间隔 8 秒，第 2→3 轮先冷静 90 秒。
第三轮是"补救轮"，**只在时间窗内才跑**——过了 `signin_time_end` 再点也没意义，会直接收工发失败通知。

## 二、首次安装（只需做一次）

双击运行 **`3_安装定时任务(管理员).bat`**，在弹出的 UAC 窗口点「是」。
它会创建/更新名为 **「油学通每日签到」** 的 Windows 定时任务：

- 每天 **20:55** 自动运行（`install_task.ps1` 的 `$triggerTime`；鄠邑校区 20:50 开始签到，留 5 分钟余量）
- **以最高权限运行**（这样才能自动清理联想电脑管家等高权限弹窗，且任务计划触发时不会再弹 UAC）
- 仅在你登录时运行、唤醒计算机执行、用电池也运行、错过时间会补跑、最长运行 30 分钟

> 前提：到点电脑处于开机且微信已登录（你已确认满足）。电脑睡眠也没关系，任务设置了唤醒。

## 三、手动测试

- **`2_手动测试签到(管理员).bat`**：推荐。自带提权（弹一次 UAC，点「是」），和夜里自动运行环境完全一致。
- **`2_手动测试签到.bat`**：不提权版本，当前台没有高权限弹窗时也能用。

**怎么看结果**（黑窗口最后一行「退出码」）：

| 退出码 | 含义 |
|---|---|
| 0 | 签到成功，已执行关机倒计时 |
| 3 | 不在签到时段 / 今日已签到（**非签到时段测试时看到 3 属正常**，说明前面链路都走通了，只是按钮是灰的）|
| 1 / 2 | 失败 / 异常，去 `logs/` 看日志和截图 |

想完整验证「真签到 + 关机」，在签到开放时间之后运行测试 bat 即可。

### 采集界面样本（可选，为将来评估 OCR 攒素材）

双击 **`采集样本.bat`**，会提示你切到微信、翻到想记录的界面，然后按回车截屏并选标签。
**全程只截屏，不点击、不控制微信，对签到主流程零影响。**

```
[1] signed_detail   详情页 - 灰色'已签到'（确认成功的硬依据）
[2] signed_list     列表卡片 - 绿色'已签到'
[3] ended_detail    详情页 - 灰色'已结束'（最想要！OCR 要靠它区分）
[4] ended_list      列表卡片 - '已结束'
[5] not_signed      详情页 - 蓝色'签到' + 绿色'请假'（未签状态）
[6] map_page        地图定位页（底部'完成签到'）
[7] other           其他 / 说不清
```

样本存到 `logs/samples/`，标签写进同目录 `labels.csv`。该目录已在 `.gitignore` 中排除。

> 为什么需要手动采集：脚本只在**出问题时**才截图，正常签到那次可能一张都不留。
> 而「已结束」这种状态平时根本不会出现在脚本的视野里（它永远取列表最上那条当天记录）。
> 所以想攒够样本，只能靠你在界面上手动翻、手动采。

## 四、配置（config.json）

> **⚠️ 首次使用先复制示例配置**：仓库里**不含** `config.json` 和
> `notify_helper/notify_config.json`（里面有你的账号密码和飞书密钥，已被 `.gitignore` 排除）。
> 克隆后先把示例复制成真配置再改：
>
> ```
> copy config.example.json config.json
> copy notify_helper\notify_config.example.json notify_helper\notify_config.json
> ```
>
> 然后把 `config.json` 的 `wifi_username` / `wifi_password` 填成你的校园网账号密码。
> **改完不要提交这两个文件。**

| 字段 | 说明 |
|---|---|
| `search_keyword` | 搜索关键词，默认 `油学通` |
| `miniprogram_title` | 小程序独立窗口标题（用于窗口查找/置顶），默认 `油学通` |
| `miniprogram_search_icon` | 搜索结果里「油学通」图标模板路径，默认 `templates/02_miniprogram_icon.png` |
| `signin_nav_steps` | 多级入口顺序（数组）：日常管理→签到消息→每日签到→每日签到里的进入按钮，每项含 name/template/scroll，默认 21/22/23/24 四张模板 |
| `signin_time_start` / `signin_time_end` | 签到开放/结束时间（仅提示用，不硬卡），默认 `20:50` / `21:30` |
| `confidence` | 通用图像匹配置信度，默认 0.8 |
| `shutdown_after_success` | 成功后是否关机，默认 `true`（想只签到不关机改成 `false`）|
| `shutdown_delay` | 成功后多少秒关机，默认 60 |
| `keep_runs` | 自动保留最近多少次运行日志/截图，默认 30 |
| `log_dir` | 日志/截图目录 |

## 五、文件清单

| 文件 | 作用 |
|---|---|
| `signin.py` | 主程序 |
| `runtime/` | 独立绿色 Python 3.13 + 全部依赖（约 220MB）。**仓库里不含**（体积太大），需按第六节说明自建，**勿删** |
| `config.json` | 配置（**仓库里不含**，从 `config.example.json` 复制） |
| `templates/` | 图像模板：00 进入微信按钮、07 顶部搜索框、06 更新弹窗关闭 X、08 更新弹窗标题（微信通用）+ 02 搜索油学通图标、21 日常管理、22 签到消息、23 每日签到、24 进入按钮（**分辨率/缩放与采集时不同则需重新采集**）|
| `run_signin_task.bat` | 定时任务实际调用（日志写 logs/task_run.log）|
| `run_signin.bat` | 普通双击运行（带 pause）|
| `2_手动测试签到*.bat` | 手动测试（管理员版/普通版）|
| `3_安装定时任务(管理员).bat` + `install_task.ps1` | 一键安装最高权限定时任务 |
| `1_采集模板.bat` + `capture_templates.py` | 采集模板（换机器后必须先跑一次）|
| `history.py` | 签到台账：每次运行追加一行到 `data/signin_history.csv`，用来回答"这学期漏了几次" |
| `collect_samples.py` + `采集样本.bat` | 界面样本采集工具（**只截图、不操作微信**）：把当前屏幕存进 `logs/samples/` 并打标签，用于日后评估 OCR 方案或排查模板问题 |
| `self_heal.py` / `step_tracer.py` | 自愈预热、步骤轨迹（纯观察，不影响签到） |
| `notify_helper/` | 飞书通知（成功发、失败也发，卡片附近 7 天统计） |
| `wifi_helper/` | 校园网自动连接与网页认证 |

> **`data/` 是本地个人数据**：`signin_history.csv` 记录了你的每日签到时间，已在 `.gitignore` 中排除、不会上传。仓库里只有一份脱敏的 `signin_history.example.csv` 供参考。

## 六、常见问题

- **没反应 / 日志显示前台被抢占**：联想电脑管家等会在屏幕外弹出高权限模态窗。务必用「管理员」版本运行，或重新运行 `3_安装定时任务(管理员).bat` 保证任务是最高权限。
- **微信窗口大小变了会不会失效**：不会，搜索用多尺度匹配、按钮动态扫描；但请不要在脚本运行时手动拖动/缩放微信窗口。
- **微信白屏（界面整块纯白）**：微信 4.x 的已知毛病，按 Esc、窗口快速切换都可能诱发。脚本已内置白屏检测，会先等渲染、再最小化/还原强制重绘，仍不行就自动重启微信；同时脚本自身已彻底不按 Esc。若手动操作时遇到白屏，关掉微信重新打开即可。
- **模板失效（微信或油学通改版后）**：运行 `1_采集模板.bat` 按提示重新截图（尤其 02/21/22/23/24 这 5 个油学通专属模板）。
- **按钮识别与"成功硬确认"（8/31 修复，9/1 增强）**：① 只认"纯色填充率≥0.6 且不横跨贴满窗口两边"的按钮——窗口圆角外透出的桌面壁纸颜色可能被误连成大块，已据此剔除；② 严格两页状态机：蓝色"签到"在=详情页（旁边绿色是"请假"绝不点），蓝色消失后出现的绿色才是地图页"完成签到"，且只点一次；**成功必须有界面依据**——点到绿色只代表请求发出，之后会反复点详情页"基本信息"右侧的刷新按钮(↻)重新向服务器拉取状态，连续 2 次见到灰色"已签到"宽按钮（此时蓝签到、绿请假都消失）才判成功，并截图存档 `签到成功_已签到`；**单轮约 50 秒刷不出"已签到"，会冷启动小程序、把"搜索→进入→签到→确认"整套流程完整重走一遍（最多两轮，间隔 8 秒）**；只有两轮都确认不到才判**失败、不关机、保留现场**（`FAIL_未确认签到成功`）。注意区分：地图页"不在区域内"灰按钮在窗口约 0.83 高度，已签详情页灰按钮在约 0.72 高度，不会互相误判；③ 已关闭 PyAutoGUI 的鼠标四角紧急中止（FAILSAFE）。
- **地图页灰色"不在区域内"/定位漂移（9/1 晚修复）**：电脑没有 GPS，靠"周边 WiFi 热点位置库 + IP"定位，偶尔会把身边热点错定位到校外，此时绿色"完成签到"不出现、按钮是浅灰色。脚本会自动两级自愈：先每 6 秒点一次地图右下角"重新定位"；连续 3 次仍无绿色，就自动断开重连当前 WiFi，强制系统重新扫描热点、重算定位，WiFi 只重连一次，地图页总等待约 78 秒。注意：① 这只解决"人确实在校区、但 PC 定位漂移"，人不在校区无法签到；② 签到时让 Clash/VPN/加速器处于"关闭系统代理、未开 TUN"状态——任务管理器里残留 `clash-verge-service` 空壳服务并不影响，但一旦真开了系统代理或 TUN，IP 定位会跟着节点走而漂移；③ 若自愈仍失败，可手动断开重连 WiFi、改连校区网络，或直接用手机签到。
- **Python 运行时（怎么准备 `runtime\`）**：仓库里**不含** `runtime\`（那是一份约 220MB 的完整绿色 Python 3.13，太大不适合放 Git）。各 bat 都用相对路径 `%~dp0runtime\python.exe` 调用它。两种准备方式：
  1. **装 Python 后复制过去**（推荐）：装 Python 3.13，`pip install pyautogui opencv-python numpy pyperclip Pillow`，再把整个 Python 目录复制为项目下的 `runtime\`（确保 `runtime\python.exe` 存在）；
  2. **拷现成的**：从另一台已经配好的同架构电脑上，把它的 `runtime\` 整个拷过来。
  这样整个项目文件夹拷到别的同架构 Windows 电脑就能直接用，无需另装 Python。
- **想改签到时间**：改 `install_task.ps1` 里 `$triggerTime` 后，用 `3_安装定时任务(管理员).bat` 重装；时间必须晚于签到开放、早于结束，否则按钮是灰的签不上。同时把 `config.json` 的 `signin_time_start`/`signin_time_end` 改成一致。
- **平时可以把微信完全退出吗**：可以。脚本到点会自己启动微信，只要微信保持「已登录」状态即可（退出的是程序进程没问题；不要「退出登录」）。最小化到任务栏也没问题，脚本会自动还原；即使主窗被收到系统托盘，脚本也会再次启动微信把单实例主界面唤出来。
- **无人值守必须保证：不锁屏、不睡死、网络通**（脚本已尽量自动处理，但有几点需要系统设置配合）：
  - 脚本运行期间会自动调用 `SetThreadExecutionState` **阻止系统睡眠/熄屏**，进程结束自动还原；定时任务本身也勾选了"唤醒计算机运行"。
  - 但**自动锁屏脚本无法替你解锁**（也不应保存密码）：请在「设置→账户→登录选项」把"离开后要求登录"放宽，或在「电源和睡眠」里把睡眠时间设为"从不"，确保到点时停在**已登录的桌面**而不是锁屏。脚本检测到锁屏会直接判失败并记录 `FAIL_屏幕锁定`。
  - **笔记本务必接通电源**，并在「控制面板→电源选项→选择关闭笔记本盖功能」把合盖设为"不采取任何操作"（或干脆不合盖）。脚本开跑会记录是否接通电源/电量。
  - 夜里保持 WiFi 已连接且能正常上网；签到时让 Clash/VPN/加速器处于"关系统代理、未开 TUN"状态。
- **脚本内置的自动预防/自愈（按执行顺序）**：① 开跑先清掉联想电脑管家等"屏幕外幽灵弹窗"、阻止睡眠、检测锁屏、记录环境快照；② 微信白屏→等待→最小化还原重绘→仍白屏就杀进程重启；更新弹窗需先认到标题才点 X；③ 搜索进小程序自带 4 次重试、每轮都冷启动小程序避免停在旧子页；④ 定位漂移→点地图"重新定位"→仍不行自动重连当前 WiFi 并验证公网真的可达；⑤ 点完成签到后反复刷新详情页、连续 2 次见到灰色"已签到"才算成功；⑥ 单轮失败自动整套重走第二遍，两轮都失败才判失败、不关机并保留截图；⑦ 只有确认成功、且 `config.json` 的 `shutdown_after_success` 为 `true` 时才在 60 秒后关机（期间可 `shutdown /a` 取消）。
- **失败了怎么排查（run.log 怎么读）**：每次运行独立存放在 `logs/run_年月日_时分秒/`，其中 `run.log`（UTF-8）结构固定，从上到下：运行环境信息 → 环境快照 → 全程 `[里程碑]` → `[扫描]` 调试行 → 结尾"运行总结"。同目录还有每步过程截图，`FAIL_*.png` 就是失败现场；脚本判失败时一定不会关机，方便当场查看或用手机补签。

## 七、复制到别的电脑

整个项目文件夹（**必须含 `runtime\`**）原样拷过去即可，Python 和依赖不用重装、bat 用相对路径也不用改。新电脑满足以下条件：

1. **64 位 Windows**（runtime 是 AMD64；ARM/32 位不行）。
2. **装好 PC 微信并保持已登录**，且你能在这台电脑上手动打开「油学通」完成签到。微信装在任意盘/目录都行，脚本会自动探测 `Weixin.exe/WeChat.exe`；万一探测不到，把实际路径填进 `config.json` 的 `wechat_path`。
3. **人/电脑在校区定位范围内**：签到靠地图定位到校区，不在校区会定位失败，这与脚本无关。
4. 文件夹放任意路径均可（桌面、D 盘都行）。
5. 先双击 **`1_采集模板.bat`** 重新采集油学通模板；再双击 **`2_手动测试签到(管理员).bat`** 完整跑一遍确认；最后双击 **`3_安装定时任务(管理员).bat`** 在新机注册定时任务（Windows 定时任务不会随文件夹复制，必须在新机重装一次）。

> 分辨率/缩放：脚本已设置 DPI 物理像素感知、模板多尺度匹配、按钮按颜色形状动态扫描，常规 1080P/2K 屏一般可直接用。若新机日志出现"模板匹配超时"，在新机运行一次 `1_采集模板.bat` 重新采集模板即可。

## 八、许可

[MIT License](LICENSE) —— 可自由使用、修改、分发，保留版权声明即可。

> ⚠️ 请自行确认所在学校对自动化签到的规定。本项目只是本机 GUI 自动化工具，不修改任何服务端数据，也不绕过任何身份验证——账号密码仍由你本人填写并保存在本地。

## 九、已知限制（诚实清单）

这些是已知的、**有意保留**或**暂时无解**的地方，不是漏做：

1. **列表"已签短路"实际不生效**。`first_card_status_green()` 想通过识别列表卡片上的绿色"已签到"
   来提前判定今日已签，但它的 ROI 一直没对准（日志里恒为"绿像素0/红像素0"）。
   **后果**：今日已签时还要多走一遍详情页、在第 4 步空滚约 20~30 秒。
   **为什么故意不修**：修好它等于"仅凭列表一次颜色判断就直接判签到成功"，
   一旦误判就是**静默漏签**（你收到的还是绿色成功卡）。收益只有省 30 秒，风险不对等，所以保持现状——
   现在一定会走到详情页做权威核对。

2. **代码分不清按钮上的"已签到"和"已结束"**。两者都是灰色宽按钮，判定只看颜色/宽度/纵向位置，
   **不读文字**。20:50 之后如果页面还停在昨天那条"已结束"记录上（例如小程序缓存没刷新），
   可能被误判成"今天已签到"→ 假成功。
   **现有缓解**：导航固定取列表最上（当天记录）+ `before_signin_start()` 时间守卫 + 确认阶段会"退出重进"刷新。
   **OCR 评估结论（2026-09-15）：暂不引入**，理由如下——
   - **无样本可验证**：全部历史截图里只有 1 次成功运行、6 张相关图，且**一张「已结束」都没有**。
     OCR 存在的唯一意义就是区分这两个词，缺少对应样本等于无法验收，接进去只能靠赌。
   - **依赖代价**：pytesseract 要装 Tesseract 主程序 + `chi_sim` 语言包；
     PaddleOCR/easyocr 会拖 GB 级依赖。这会**破坏本项目"拷到任何 Windows 机器双击即用"的特性**。
     Windows 内置 OCR 虽零依赖，但要绕 WinRT、中文小字质量一般、跨版本行为不稳。
   - **实测对比度偏低**：灰色「已签到」是白字（灰度 254）在浅灰底（灰度 204）上，
     亮度差仅 50/255，属于 OCR 容易出错的那类场景。
   - **风险方向不利**：一旦 OCR 把「已结束」认成「已签到」，会**直接返回成功、推绿色卡片、执行关机**
     —— 这正是最不该发生的"假成功"，且因为几何条件也过了，更难被发现。
   - **该风险尚未被证实存在**：新增的台账（`data/signin_history.csv`）已在记录每日真实结果，
     若真出现假成功，连续失败告警会暴露它。**先用数据确认问题存在，再考虑方案。**
   - 若将来要接，唯一可接受的形式是**「仅否决权」**：OCR 只在已经准备判成功时复核一遍，
     读到「已结束」才推翻判定；读不出就维持原有几何判定。这样它识别错只会导致"多跑一轮"（可恢复），
     而不是"假成功"（不可恢复）。
   - **采样工具已就位**：`采集样本.bat` 可把界面截图存进 `logs/samples/` 并打标签。
     等你顺手攒到几张「已结束」的截图，这条才具备重新评估的条件。

3. **搜索框模板对窗口长宽比敏感**。微信窗口一宽，聊天列表列变宽，顶部搜索框会被拉成另一个长宽比，
   而多尺度匹配是**等比缩放**、改不了长宽比，所以怎么缩放都匹配不上。
   **已处理**：开跑时若窗口宽度 >1400px 会自动缩回 1118×1715。

4. **被别的窗口挡住会失败**（脚本匹配的是**屏幕截图**，不是窗口内部）。
   **已处理**：开跑时会自动最小化盖在微信上的窗口（含 QQ音乐桌面歌词、聊天窗口等）。
   **残留风险**：如果在"正在搜索"的那几秒里突然弹出新窗口，来不及清，那一轮会失败（有第二轮重试兜底）。

5. **脚本会最小化自己的控制台窗口**（因为它确实浮在微信上）。功能无影响——完整日志在
   `logs/run_*/run.log`，不看控制台也能排查。

6. **`logs/task_run.log` 不会轮转**。它是 .bat 用 `>>` 重定向写的、运行期间一直被占用，
   脚本删不掉也转不了。约 30KB/天、一年约 11MB，暂不处理。
   （按天的 `signin_YYYYMMDD.log` 已按 `keep_days` 自动清理。）

7. **极端情况未穷举**。已实测：窗口 863/1118 宽、被拖大到 2906 宽、被 1~2 个窗口遮挡。
   未实测：超小窗口、最大化按钮状态、多个窗口同时遮挡等组合——这些会走重试和重启微信兜底，
   失败时飞书会推红色卡片，不会静默漏签。